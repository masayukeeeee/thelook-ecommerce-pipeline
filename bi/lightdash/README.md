# Lightdash local setup

## Preparation

* .env.exampleを更新する
* motherduckのアカウントを作成し、Access Tokenを発行する
  * `export MOTHERDUCK_TOKEN=xxxx`などで環境変数にセットする
* `docker compose up -d` でアプリケーションを起動
* `http://localhost:8080` でLightdashにアクセス
